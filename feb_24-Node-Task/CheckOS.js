const os = require("os");

// Check OS ARCHITECTURE
console.log("OS Architechture", os.arch());

// CHEACK OS PATH

console.log("OS CPU running", os.cpus());

//cheak HOME dir
console.log("Home Dir name", os.homedir());

//check HOST name

console.log("Host Name is ", os.hostname());

//OS type name

console.log("OS Type name", os.type());

// os userInfo

console.log("User Information", os.userInfo());
